# Daily-AI 总体视觉标准

## 1. 视觉定位

学生端采用“**暖白学习手帐 × 轻量创客社区**”视觉体系。公开入口为五区社区品牌落地页，无传统顶部导航；登录社区强调真实交流、学习记录与作品；操作型实训保留深色沉浸模式。品牌与浏览器标题统一为“Daily-AI”，口号为“破盒启智、交互赋能 数训筑基、共创未来”；标识采用橙色方块中的白色 D。

口号在 Logo 品牌区固定两行：“破盒启智、交互赋能”与“数训筑基、共创未来”，第一行末尾不带顿号；单行配置与无障碍名称以空格连接。紧凑移动顶栏不显示口号，完整口号放在更多菜单或页脚。

## 2. 色彩体系

- **品牌主色：** 活力橙 `#FF4D1F`，用于主按钮、当前导航、重点数字和行动入口。
- **页面背景：** 暖白 `#FBFAF7`，侧栏 `#FCFAF6`，卡片 `#FFFFFF`，边框 `#ECE5DE`。
- **文字：** 标题 `#191817`、正文 `#4C4946`、辅助信息 `#686460`；`#9B958F` 只用于非关键信息，作者身份、推荐理由、数量和任务名称不得使用浅色装饰字。
- **辅助色：** 紫色代表大模型，绿色代表 AI Agent，蓝色代表模型部署，黄色代表智能硬件，青绿色代表 AI 安全。
- 彩色只用于重点信息，页面主体仍以暖白、黑灰和留白为主，禁止大面积高饱和渐变。
- `frontend/src/styles/tokens.css` 是唯一 Token 定义处。新样式使用 `--amc-*`；旧变量仅作为兼容别名，`--muted` 映射可读的 secondary 色。

## 3. 页面风格

社区采用左侧导航、中央信息流、右侧学习辅助三栏。中央背景与页面一致，动态卡彼此独立，不使用连体表格式分隔。右侧顺序固定为“我的学习节奏”“正在讨论的话题”“一起向前的伙伴”；品牌口号仅作小号页脚。学习、资源、搜索等 wide 页面不占用右侧栏。

插画仅用于发布卡、计划卡、关联内容、空状态与少量背景装饰，不遮挡文本或按钮。课程、资源、实训内容仍使用既有真实封面。

## 4. 字体与排版

字体顺序为 `Inter`、`PingFang SC`、`Noto Sans SC`、`Microsoft YaHei`、系统无衬线。中文保持自然换行，禁止单字竖排。

| 层级 | 字号 |
| --- | --- |
| 登录宽页标题 | 28px |
| 区块 / 动态标题 | 20px |
| 卡片标题 | 17px |
| 正文 / 动态正文 | 14px / 15px |
| 时间、身份与数量 | 至少 12px |

动态标题最多两行，正文行高 1.75；公开门户保留较大的营销标题，不将社区尺寸机械套用到 Hero。

## 5. 卡片与组件

- 普通卡片 17px 圆角，控件 11px，小标签 8px，弹层和 Hero 22px，圆形头像与胶囊按钮单独处理。
- 卡片阴影 `0 8px 28px rgba(55,39,28,.045)`，悬停只轻微加深阴影或边框。
- 主按钮使用橙色实心样式，次按钮使用白底浅灰描边。
- 同一区域只保留一个最强主操作，避免橙色按钮过多。
- 同类卡片的封面比例、标题高度、信息顺序和按钮位置必须一致。
- `CommunityAvatar` 统一头像；`FollowButton` 统一关注态；`CommunityBindingCard` 区分课程、主题、实训、资源、文章与挑战；`CommunityEmptyState` 统一空状态。所有交互继续使用既有业务组件与 Store。

## 6. 深色实验室

实训工作台采用深蓝黑背景、分层面板和低饱和蓝紫描边。绿色仅表示运行成功，紫色表示流程节点或当前选择，橙色用于关键操作。终端、工作流、运行日志、任务指导和结果反馈必须分区清晰。深色模式只用于操作型页面，不扩展到全站。

## 7. 图片与图标

图标统一使用 `AppIcon`，线宽 1.8，禁止混用 Unicode 图标。插画采用柔和 3D、低饱和半透明材质，不带文字、水印或 Logo；图片不得拉伸变形。

正式资源位于 `frontend/src/assets/community/`，由 `manifest.ts` 集中导出：6 张独立插画、8 张独立头像均为本地 WebP，保留透明通道。头像 256×256；发布插画 1200×520；其余插画宽 360–560px。`img` 显式宽高，非首屏资源懒加载。

落地页资源位于 `frontend/src/assets/landing/`：5 张本地 WebP 与 6 个可编辑 96×96 SVG 由独立 manifest 导出。功能 SVG 使用蜜桃橙渐变与统一圆角，其余功能图标继续复用 AppIcon；不嵌入位图或外部引用。标题、按钮和预览卡均为真实 HTML。

头像优先使用接口 `avatar` / `avatarUrl`，加载失败再使用显式 `avatarKey` 或用户名稳定映射，最终才回退文字。不得随机分配或按列表下标分配头像；头像仅改变表现，不改变账号身份。

## 8. 动效原则

控件、导航、卡片、展开反馈分别采用 140 / 160 / 180 / 220ms。信息流不使用弹跳、缩放或悬停位移。保留骨架和工作台状态动效；`prefers-reduced-motion: reduce` 下关闭非必要动画与平滑滚动。

## 9. 响应式与一致性

社区外壳最大 1480px，1440px 视口左右约 48px、1280px 左右 32px；仅调整外部留白，原三栏、卡片和滚动逻辑不变。使用 `minmax(0, …)` 分配中央列，不同时强制“680px 中栏 + 固定侧栏”导致窄桌面溢出。公开落地页最大 1280px，五区顺序固定；桌面 Hero 双栏，平板上下排列，手机仅保留两张主要预览卡。

| 视口 | 左栏 / 右栏 | 内容策略 |
| --- | --- | --- |
| ≥1500px | 288px / 350px | 间隔 24px，中央列最大 860px |
| 1280–1499px | 240px / 304px | 间隔 22px，中央列弹性缩小 |
| 992–1279px | 212px / 隐藏 | 双栏，内容不横向溢出 |
| 768–991px | 76px / 隐藏 | 图标导航 |
| <768px | 隐藏 / 隐藏 | 顶部栏、底部导航，单一发布入口 |

wide 内容最大 1180px，依据实际容器宽度在 1050px / 700px 收拢内部网格，避免只判断视口而忽略侧栏占宽。短屏保留侧栏自身滚动，不裁掉账户操作。正文滚动与页面位置恢复仍使用既有机制。

样式入口顺序：Token → foundations → components → public/workspaces → community 职责文件 → pages → responsive。社区响应式最后加载，保证明确覆盖旧页面媒体规则。禁止重新引入 `legacy.css`、大而全 `community.css` 或末尾叠加修复块。

## 10. 视觉红线

- 不做传统高校门户网站。
- 不做 Element Plus 默认后台风格。
- 不使用大面积企业蓝或赛博朋克霓虹。
- 不混用多套字体、图标和插画风格。
- 不使用无法真实开发的异形布局。
- 不让装饰效果影响内容可读性和功能操作。
