# 心得体会

## 一、GitHub 仓库连接
- 本次实训我 fork 了老师的仓库，并在本地完成了项目的完整部署（Node.js + PostgreSQL + 前端构建）。
- 通过 `git remote add origin <仓库地址>` 将本地项目与我的 GitHub 仓库 `AI-Learning-Hub` 关联，使用 `git remote -v` 验证绑定成功。
- 凭据由 Git Credential Manager 统一管理，`git push` / `git pull` 无需重复输入账号密码。

## 二、本地项目部署实践
- 学生端与管理端使用 Vue 3 构建，后端使用 NestJS，数据库使用 PostgreSQL。
- 完成了数据库迁移（`prisma migrate deploy`）与初始化（`bootstrap`），并配置了 `.env` 环境变量。
- 构建生产包后，通过本地静态服务与反向代理将 `/api/v1` 请求转发到后端，实现了学生端（8080）与管理端（8081）的完整访问。
- 部署过程中还排查并修复了“注册引导页下一步按钮置灰”的问题——原因是数据库缺少已发布的学习方向，补齐数据后恢复正常。

## 三、提交与推送
- 使用 `git add` 暂存文件，`git commit -m "提交说明"` 保存版本，`git push -u origin <分支>` 推送到远程仓库。
- `.gitignore` 正确忽略了 `.env`、`node_modules/`、`dist/` 等文件，避免把密钥和构建产物误传到 GitHub。
- 推送前用 `git status` 检查变更，确保只提交需要的内容。

## 四、Pull Request 流程
- 在本地新建分支完成修改后，将分支推送到自己的 fork 仓库。
- 然后在 GitHub 页面发起 Pull Request，将新分支合并到 `main`，完成一次完整的协作开发闭环。
- PR 让变更可以被审阅、讨论，也方便老师检查提交内容。

## 五、收获与总结
- 掌握了从本地到远程的完整 Git 协作流程：clone / fork → 分支开发 → commit → push → Pull Request。
- 理解了 `.gitignore`、凭据管理、分支隔离在真实项目中的重要性。
- AI 辅助开发（Vibe Coding）可以显著提升效率，但仍需自己把握工程逻辑与安全边界。