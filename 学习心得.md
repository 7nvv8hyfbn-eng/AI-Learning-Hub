# Git & GitHub 实训学习心得

## 一、开源与闭源
- **开源**：源代码全部公开，任何人可以查看、使用、修改，GitHub上很多项目属于开源。
- **闭源**：看不到源代码，例如Windows程序、商业游戏。
- **MIT协议**：开源许可协议，允许二次开发、商用，只需要注明原作者来源。

## 二、Git的作用
1. **多人协作**：多人同时修改同一个项目，分工开发，最后把代码合并在一起。
2. **版本存档**：相当于游戏存档，代码改坏可以回滚到之前版本，防止项目直接报废。

## 三、核心概念理解（游戏存档比喻）
|概念|通俗理解|
|---|---|
|commit|存档，保存当前项目快照，写上备注|
|push|推送，把本地存档上传到云端GitHub|
|pull|拉取，把网上最新代码下载到本地电脑|
|branch|分支，从主干分出独立工作线，互不干扰|
|main/master|主干，项目主线，所有分支最后合并回来|
|Fork|把别人整个仓库复制一份到自己账号下|
|PR(Pull Request)|请求合并，做完功能申请合并到主干|
|Review|审查，组长检查提交内容，决定是否合并|

## 四、多人协作完整工作流程
1. 组长创建总仓库作为主干。
2. 成员Fork总仓库到自己账号。
3. 在自己仓库新建分支进行开发。
4. 本地完成代码，执行`commit`存档，再`push`推送到自己远程仓库。
5. 发起PR，请求合并到组长的主干仓库。
6. 组长Review审查，通过就合并，有问题就退回修改。
7. 合并之后所有人重新拉取最新主干继续开发。

## 五、增量与全量提交
- **全量推送**：上传全部文件，审查困难，不推荐。
- **增量推送**：只提交自己改动过的文件，方便别人查看修改内容，实训优先使用增量提交。

## 六、本地项目连接GitHub远程仓库
想要把电脑本地项目和网上GitHub仓库关联，有两种实现方式。

### 方式1：命令行方式
1. 复制GitHub仓库的`.git`远程地址。
2. 终端进入本地项目文件夹，执行`git init`初始化仓库。
3. 使用`git remote add origin 仓库地址`完成本地与远程仓库绑定。
4. 使用`git remote -v`验证连接是否生效。
5. 依次执行`git add .`、`git commit -m "提交备注"`、`git push -u origin main`将本地内容上传到GitHub。

> 如果提示`remote origin already exists`，代表已有旧远程连接，需要先执行`git remote remove origin`删除旧配置。

### 方式2：简单图形化工具 GitHub Desktop
不用记忆复杂git命令，适合新手。
1. 下载安装GitHub Desktop，登录自己的GitHub账号。
2. 选择克隆（Clone）目标仓库，选择电脑本地存放路径，克隆完成就自动完成本地和远程仓库的连接绑定。
3. 在本地文件夹修改文件，软件会自动识别改动。
4. 填写提交备注，执行`Commit`保存本地版本，再点击`Push origin`上传到网上仓库。

> 注意：网页直接拖拽上传文件只是单纯上传文件，**不算完成项目和仓库的git连接**，不能满足实训考点要求。

## 七、AI与Vibe Coding感悟
今天也学习了AI、Vibe Coding（氛围编程）：用自然语言描述需求，AI生成代码，快速做原型Demo。
但AI生成的代码会存在隐患，会积累技术债务，不能完全无脑信任AI。AI是辅助工具，人依然要把控逻辑、做代码审查。

## 八、实训实操收获
本次实操学会注册GitHub、新建`.md`文档、理解Fork和PR提交作业的完整流程。
学会了本地项目连接GitHub远程仓库的命令行操作，也掌握了GitHub Desktop图形化简易操作方法。
明白了：**仅仅把文件放到自己仓库不算交作业，必须发起PR，老师那边才能看到我的提交**。

## 九、总结
Git、GitHub不只是写代码用，也可以用来协同写文档、整理笔记。不用死记全部命令，重点要理解协作流程。
本地和远程仓库连接是很关键的一步，可以用命令，也可以使用图形工具简化操作。后续我会多加练习，熟练掌握分支、提交、推送、PR整套操作。
