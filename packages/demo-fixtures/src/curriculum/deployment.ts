import { illustration as img, lesson as l, type CourseCurriculum } from './types'

export const deploymentCurriculum: CourseCurriculum[] = [
  { slug: 'linux-basics', sources: [
    { title: 'GNU Bash：命令、管道与重定向', url: 'https://www.gnu.org/software/bash/manual/bash.html' },
    { title: 'GNU Coreutils：文件权限', url: 'https://www.gnu.org/software/coreutils/manual/html_node/File-permissions.html' },
  ], chapters: [
    { title: '知道自己正在操作哪里', lessons: [
      l('终端、Shell 与命令不是同一个东西', [
        '终端像你和电脑对话的窗口，Shell 负责理解输入的命令，再调用程序做事。小雪输入一条查看目录的指令，屏幕上的结果来自实际程序，而不是终端窗口自己猜出来的回答。',
        '一条简单命令通常包含程序名、选项和参数。空格、引号与特殊符号会影响Shell怎样解释输入。先读懂命令用途和目标，再执行；从网页复制的命令可能包含下载执行、覆盖或删除操作，不能只因为看起来像教程就照做。',
      ], ['在“ls -l notes”中，哪个是程序，哪个是选项，哪个是目标？', 'ls是程序名，-l是以详细格式显示的选项，notes是要查看的目标。执行前仍应确认当前目录和notes指向哪里。'], '终端提供窗口，Shell解释命令，程序执行具体工作。'),
      l('目录树与相对路径：先定位再操作', [
        '找一份实验笔记，可以从资料柜总入口一路走到具体格子，也可以从当前所在格子继续找。文件系统中的绝对路径与相对路径有类似区别，小雪先确认当前位置，再确定目标文件。',
        'Linux 中以斜杠开头的路径从根目录出发；相对路径从当前工作目录解释，点表示当前目录，两个点表示父目录。目录树只是组织方式的类比，符号链接等机制还可能把路径指向别处。处理带空格的路径时应正确引用。',
      ], ['同一条“cat notes.txt”在两个目录运行，为什么可能显示不同文件或报不存在？', 'notes.txt是相对路径，目标取决于当前工作目录。先用pwd和ls核对位置，再选择正确路径，不能假定终端一直停留在原目录。'], '相对路径依赖当前位置，执行文件操作前先确认路径。', img('linux-basics', 'filesystem', '从共同根节点沿文件夹分支寻找文件，帮助理解目录层级；实际路径还需考虑符号链接。'), undefined, ['bash', 'pwd\nls -la\nls -l "notes"']),
    ] },
    { title: '读懂输出与访问权限', lessons: [
      l('管道和重定向会改变数据流向', [
        '小雪查日志时，先让一个程序读内容，再让另一个程序筛选需要的行。管道就像把前一道工序的输出交给下一道工序。重定向则把输出送进文件，方便留下结果。',
        '竖线连接前一命令的标准输出与后一命令的标准输入；错误输出有独立通道。大于号通常创建或截断目标文件，两个大于号通常追加。它们不只是排版符号，可能改变文件内容；保存前确认目标，避免覆盖原始数据。',
      ], ['把检查结果保存到已有笔记文件时，为什么要先理解“>”与“>>”的差别？', '前者可能清空原文件后写入，后者追加。若目的只是查看结果，先不要重定向；需要保存时选独立输出文件并确认路径。'], '管道传递输出，重定向可能写文件，先看清数据最终流向哪里。'),
      l('读、写、执行权限分别控制什么', [
        '资料柜里有的同学能看笔记，有的能修改，少数能运行实验工具。小雪按用途分配权限，而不是给每个人一把总钥匙。文件权限把访问能力分给所有者、所属组和其他人。',
        '常见权限r、w、x表示读取、写入、执行；对于目录，x主要关系到路径穿行，含义与普通文件不同。权限还会受到上级目录、ACL等机制影响。遇到拒绝访问，应找出缺少哪项必要权限，不要用全开放权限掩盖问题。',
      ], ['一份只需本人阅读的配置文件，是否应为了方便给所有人读写执行？为什么？', '不应。应只给实际需要的主体必要能力；配置文件通常不需要执行权限。先核对所有者与用途，再采用合适权限，避免泄露或被意外修改。'], '权限应匹配主体和操作，能访问不等于应该开放给所有人。', img('linux-basics', 'permissions', '眼睛、铅笔和齿轮分别提示读、写、执行能力；实际权限由系统检查，目录权限另有语义。')),
    ] },
    { title: '从进程与日志判断运行情况', lessons: [
      l('进程、端口与服务状态要一起看', [
        '服务器上的程序可能还在运行，却没有对外提供正确响应。小雪分别确认进程是否存在、预期端口是否监听，以及接口能否完成真实请求，避免只看一个绿色状态就宣布成功。',
        '进程是运行中的程序实例，端口帮助系统把网络连接交给对应服务。一个服务可能启动多个进程，也可能因配置错误监听了不同地址。停止操作应指向确认归属的进程或服务，不能为了释放端口批量终止不明程序。',
      ], ['程序进程存在但网页无法打开，应继续检查哪两类证据？', '检查监听地址和端口是否符合预期，以及网络或反向代理是否能到达；再看应用日志和实际请求响应。进程存在只说明进程尚未退出。'], '服务可用需要运行状态、网络入口和实际功能相互印证。'),
      l('用最小范围的日志排查问题', [
        '一份日志有几万行，小雪先按出错时间和请求范围定位，再读取前后相关内容。这样既容易找到原因，也减少把无关账号信息或密钥带进问题报告的机会。',
        '排查时记录命令、环境和必要错误信息，区分已确认事实与推测。一次只改一个有依据的因素，检查结果后再继续。任务结束时清理自己创建的临时文件和无用进程，保留能复现问题的最小证据。',
      ], ['接口刚才失败，为什么不应该把整份服务器日志直接贴到公开讨论区？', '整份日志可能含个人信息、路径或凭据，且大量无关内容妨碍定位。应提取对应时间的必要错误上下文并脱敏，说明实际症状和已做检查。'], '日志排查从具体时间和症状收敛，保留必要证据并保护敏感内容。'),
    ] },
  ] },
  { slug: 'docker-models', sources: [
    { title: 'Docker：认识容器', url: 'https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/' },
    { title: 'Docker：持久化数据卷', url: 'https://docs.docker.com/engine/storage/volumes/' },
  ], chapters: [
    { title: '把环境变成可重复的交付物', lessons: [
      l('标准行李箱类比容器环境', [
        '你的程序在自己电脑上能运行，换到同学电脑却缺少依赖。小雪把程序和需要的运行文件按固定方式装进标准行李箱，让使用者按同样入口启动。这个类比解释了容器化为什么有助于交付环境。',
        '容器本质上是带隔离边界的进程，通常共享宿主机内核，并不是一台完整虚拟机。镜像也不会抹平CPU架构、驱动、GPU和外部配置差异。可移植意味着减少环境差异，仍要核对实际平台兼容性。',
      ], ['装入容器后，为什么仍要确认服务器架构与GPU驱动？', '镜像和程序需要兼容目标平台，GPU访问也依赖宿主机相关支持。容器没有把任意硬件自动变成相同环境。'], '容器减少运行环境差异，但不会消除硬件与系统兼容边界。'),
      l('Dockerfile、镜像、容器各扮演什么角色', [
        '小雪先写打包配方，再制作一份标准样品，最后按样品打开实际工作的箱子。对应到Docker，Dockerfile描述构建步骤，镜像保存运行所需内容，容器是按镜像启动的实例。',
        '同一镜像可以启动多个容器，各自拥有运行状态和可写层。修改正在运行的容器，并不会自动修改构建配方或形成可追踪的新镜像。正式变更应回到源码与构建流程，记录镜像标识，再验证新实例。',
      ], ['在运行中的容器里临时安装依赖后，删掉容器重建可能发生什么？', '如果依赖没有进入构建过程或持久配置，新实例可能再次缺少它。应把必要变更写入可复现构建定义，而不是依赖临时容器状态。'], '配方定义构建，镜像保存产物，容器承载运行状态。', img('docker-models', 'container', '配方卡、标准镜像箱与运行实例依次相连；行李箱只是环境组织的类比。'), ['Dockerfile构建定义', 'Image镜像', 'Container运行实例', 'Service对外响应']),
    ] },
    { title: '把状态和入口放对位置', lessons: [
      l('数据卷让运行实例与资料分离', [
        '小雪更换工作箱时，不希望把积累的课程笔记一起扔掉，于是把资料放进独立柜子。容器可写层随实例生命周期变化，重要数据应放在明确管理的持久化存储中。',
        'Docker volume 是独立管理的数据存储，bind mount 则映射宿主机指定路径。选择哪种方式要看部署约定和管理需求。持久化不等于备份：误删、错误迁移和应用写坏仍可能损失数据，需要独立备份与恢复验证。',
      ], ['数据库已挂载数据卷，是否就可以省略备份？请举出一个数据仍会丢失的场景。', '不能省略。错误删除记录、损坏数据或误删卷都可能影响持久数据；应保留独立备份并验证能恢复。'], '数据卷把状态从容器实例中分离，备份还需要单独建立。', img('docker-models', 'volumes', '可替换运行箱连接独立资料柜；保留数据卷可以跨实例保存资料，但它本身不是备份。')),
      l('端口映射与配置决定外部如何访问', [
        '箱子里的服务有自己的窗口，宿主机还需要安排一个对外入口。小雪核对容器内监听端口、宿主机映射端口和访问地址，防止把“内部已经启动”误认为“校园网都能访问”。',
        '端口发布会改变网络可达范围，应按用途绑定地址并结合现有网络控制。配置和密钥应通过部署约定提供，不写进镜像或公开源码。配置错误、服务只监听回环地址或映射不匹配，都可能导致入口不可达。',
      ], ['容器内服务监听8000，而同学访问宿主机8088，至少需要核对哪些配置？', '核对8088到容器8000的映射、服务监听地址、宿主机地址与网络控制，并用实际请求验证。端口数字不同本身并不是错误。'], '容器内部监听与宿主机公开入口是两层配置，需要逐层核对。'),
    ] },
    { title: '更新时保护真实业务', lessons: [
      l('健康检查要接近服务真实职责', [
        '小雪看到容器状态为运行中，还会试一次服务的关键请求。进程活着、健康检查通过、用户能完成业务是逐层增强的证据，不能相互随意替代。',
        '健康检查应有明确超时和适当频率，避免反过来给服务施压。还要观察重启次数、错误日志与资源使用，确认模型文件和依赖服务可用。对推理服务，应至少验证实际输入能够得到符合契约的输出。',
      ], ['健康接口返回200，但推理接口因缺少模型文件失败，可以宣布部署完成吗？', '不能。健康检查没有覆盖关键依赖或业务路径，需要修复模型文件与配置，并验证实际推理请求后再判断。'], '容器健康是检查入口之一，部署验收还要验证关键业务路径。'),
      l('用固定版本更新并保留回退路径', [
        '更新课程平台时，小雪先确认当前版本、备份数据，再用明确镜像和提交构建新版本。验证成功后才切换入口；如果异常，能找到之前的配置和产物进行恢复。',
        '代码更新不应顺手重置数据库或导入整套演示数据。迁移要明确影响与兼容性，静态站点还可能需要保留旧分包供已打开的页面使用。回退应用不一定自动回退数据变更，因此发布前必须理解两者边界。',
      ], ['新前端发布后，仍打开旧页面的同学加载某个功能失败，为什么要检查旧静态分包是否保留？', '旧页面可能仍引用上一版带哈希的分包；如果发布时删除它们，就会出现加载失败。应按部署策略保留兼容资源并验证缓存行为。'], '可靠更新绑定固定产物、保护数据，并提前验证实际可用的恢复路径。'),
    ] },
  ] },
  { slug: 'fastapi-inference', sources: [
    { title: 'FastAPI：请求体与数据模型', url: 'https://fastapi.tiangolo.com/tutorial/body/' },
    { title: 'FastAPI：并发与异步', url: 'https://fastapi.tiangolo.com/async/' },
  ], chapters: [
    { title: '把模型能力变成明确接口', lessons: [
      l('API 像按约定接单的食堂窗口', [
        '在食堂窗口点餐，你按菜单提供菜品和数量，窗口按约定交回结果。小雪把模型能力封装成API，让其他程序按照固定格式请求服务，而不必知道模型内部如何运行。',
        'HTTP接口通常有地址、方法、请求数据和响应约定。FastAPI是构建Python Web API的框架，它帮助组织路由、数据校验与接口描述；模型推理仍由你接入的代码或服务完成。食堂类比帮助理解契约，不代表请求一定立即成功。',
      ], ['为什么“把模型文件放到服务器”还不等于已经提供可调用的API？', '还需要运行服务、定义请求与响应、处理模型调用并配置可访问入口。文件存在只是其中一个条件，调用者需要可验证的接口契约。'], 'API提供调用约定，框架组织服务，模型计算需要明确接入。'),
      l('从请求数据走到响应结果', [
        '小雪设计一个短文本分类接口：调用者提交text，服务返回类别和必要状态。先把最小输入输出说清楚，再连接模型，比一开始就塞进几十个可选参数更容易理解和维护。',
        '请求体可以用Pydantic数据模型声明字段与约束，FastAPI据此读取和校验。模型输出还应转换成稳定响应，避免把内部对象、文件路径或异常堆栈直接暴露出去。字段校验通过，不意味着文本内容可信或用户有权执行所有操作。',
      ], ['text缺失、text为空、模型超时这三种情况，应该都返回同一个正常分类结果吗？', '不应该。缺失和空值应按输入契约处理，模型超时应返回可识别的失败响应；不能用看似成功的默认类别掩盖故障。'], '请求和响应都要有契约，失败情况也属于接口设计。', img('fastapi-inference', 'request', '请求卡经过处理得到响应卡；服务应核对输入并明确表达成功或失败。'), ['读取请求', '校验输入与身份', '调用推理能力', '整理响应', '返回状态与结果']),
    ] },
    { title: '正确接入耗时的模型计算', lessons: [
      l('加载模型与处理请求分开考虑', [
        '每来一个顾客就重新搭一次厨房，会让窗口很慢。小雪在服务生命周期中安排模型加载，让请求处理复用已经准备好的资源，同时确认多个进程会各自占用多少内存。',
        '模型初始化、共享状态和并发访问要按实际库的约束管理。不要假定增加Web工作进程就能免费提升吞吐：每个进程可能加载独立模型并占用显存。启动、就绪和关闭阶段都要有清楚行为，异常时释放本次资源。',
      ], ['一张GPU刚好容纳一份模型，直接启动四个各自加载模型的工作进程有什么风险？', '可能重复占用显存导致加载失败或运行时内存不足。应先测量资源，再选择合适的进程与推理服务架构。'], '模型生命周期与工作进程配置必须一起考虑，不能盲目复制模型实例。'),
      l('异步等待不等于计算自动并行', [
        '窗口等待另一项服务回复时，可以先处理别的接待工作；真正做一道耗时计算仍需要计算资源。小雪把网络等待与CPU或GPU计算分开，避免以为写上async就能让任何任务变快。',
        '异步适合配合可等待的I/O操作；阻塞调用放在异步路径中仍可能阻碍其他请求。计算任务需要与框架兼容的执行安排、队列和资源限制。并发量增加到一定程度会带来排队，必须观察响应时间、错误率与资源消耗。',
      ], ['模型计算阻塞了事件循环，单纯把函数声明改成async为什么可能无效？', '声明不会自动把同步计算移到其他执行资源。需要识别阻塞部分，按实际框架和模型库选择适当处理方式，并测量并发下的结果。'], '异步改善等待的组织方式，计算性能仍受实际执行资源限制。', img('fastapi-inference', 'concurrency', '排队任务与正在处理的任务分开管理；并发接单不代表可以无限同时计算。')),
    ] },
    { title: '让调用方知道服务是否可靠', lessons: [
      l('超时、限流和错误格式保护双方', [
        '同学的客户端不应一直等一个永远没有结果的请求，服务器也不应接受无限长文本和无限排队。小雪为输入规模、等待时间和并发量设定与资源相符的边界，给失败留下明确出口。',
        '客户端和服务端的超时要协调，重试策略要考虑重复计算与副作用。错误响应应稳定、可理解，详细内部错误留在受控日志中。若有身份与权限要求，应由服务端执行校验，不能只在网页隐藏按钮。',
      ], ['一次请求超时后，客户端立刻无限重试会造成什么问题？', '可能堆积重复计算，加重服务器负载并延长排队。应采用有次数和间隔限制的重试，确认是否适合重试，并向用户表达失败状态。'], '可靠接口要限制资源与等待，并给调用方明确、稳定的失败信息。'),
      l('部署验收要用真实契约走一遍', [
        '小雪先试一个合法短输入，再试缺失字段和超长输入，最后验证暂时不可用时的响应。这样能看出接口是否真的按承诺工作，而不是只证明文档页面能够打开。',
        '同时核对入口地址、访问控制、健康与就绪检查、日志脱敏和资源使用。部署前固定代码与模型标识，保留恢复路径。演示接口不能因为测试方便就把调试信息、管理能力或原始敏感输入公开。',
      ], ['一个最小推理接口验收为什么既需要成功案例，也需要失败案例？', '成功案例验证主路径，失败案例验证契约与边界是否生效。只测正常输入无法发现无限等待、错误伪装成功或信息泄露等问题。'], '接口验收应覆盖正常输入、错误输入和依赖故障，而不只看服务首页。'),
    ] },
  ] },
  { slug: 'vllm-basics', sources: [
    { title: 'vLLM：推理服务文档', url: 'https://docs.vllm.ai/en/latest/' },
    { title: 'PagedAttention 论文', url: 'https://arxiv.org/abs/2309.06180' },
  ], chapters: [
    { title: '认识语言模型服务的工作负载', lessons: [
      l('推理引擎负责高效组织模型运行', [
        '一个同学问问题时，模型只需处理一份输入；全班同时使用时，还要安排排队、显存和生成进度。小雪把这类工作交给推理服务引擎，自己关注接口与业务规则。',
        'vLLM面向语言模型推理与服务，通过调度和内存管理等机制组织计算。它不是模型本身，也不替代权限、输入检查或业务评测。具体模型、硬件与特性支持会变化，部署时要查相应版本文档，不能根据产品名称假定全部兼容。',
      ], ['“换用vLLM后模型一定回答更准确”为什么不是合理结论？', '推理引擎主要改变运行与服务组织方式，回答质量还取决于模型、输入、采样和配置。必须分别评估质量与服务性能，不能把吞吐优化当成知识提升。'], '推理引擎优化运行组织，模型能力与业务正确性仍需单独验证。'),
      l('Prefill 与 Decode 的工作不同', [
        '小雪先读完整份问题和资料，再逐步写下回答。语言模型服务也常区分处理输入的阶段与逐步生成新token的阶段。长输入和长输出给系统造成的负担并不完全一样。',
        'Prefill处理提示中的token并建立相关中间状态；自回归Decode逐步生成后续token。首个token等待时间与之后的生成速度因此要分别观察。这个阅读写作类比只说明阶段差异，模型并不像人一样先形成完整答案再抄出来。',
      ], ['长篇资料配一个很短的问题，为什么仍可能让首个token等待较久？', '服务需要先处理较长输入，还可能受到排队和调度影响。输出最终很短，不代表输入处理开销也很小。'], '首字等待与后续生成是不同观察点，输入长度和输出长度都影响服务负担。'),
    ] },
    { title: '缓存与调度怎样帮助服务', lessons: [
      l('KV Cache 保存前文的中间计算结果', [
        '写到一句话的后半段，小雪不想每次都把前面的笔记重新整理，于是把已算好的前文信息放进抽屉。KV Cache帮助自回归生成复用已有token的注意力键和值，减少重复计算。',
        '缓存保存的是中间张量，不是预先背好的完整答案，也不是长期知识库。随着上下文与并发请求增加，它会占用显存。这里不用背公式，先记住模型权重和KV缓存是两类不同开销；抽屉类比不等于真实内存按纸卡存放。',
      ], ['为什么同一个模型在短对话能运行，换成长上下文或高并发后可能显存不足？', '除了权重，还需容纳更多KV缓存和其他运行开销。只估算模型文件大小会低估实际需求，应按上下文与并发一起测量。'], 'KV Cache复用前文中间结果以减少重复计算，同时需要额外内存。', img('vllm-basics', 'kv-cache', '抽屉中保存前文对应的计算卡，新token继续加入；缓存不是答案库，也不是永久记忆。')),
      l('连续批处理让空出的计算位置继续工作', [
        '小组任务长度不同，短任务做完后，如果必须等最慢的同学才能接新任务，就会浪费空位。小雪在合适时机把新请求放进空出的处理位置，让仍在生成的请求继续前进。',
        '连续批处理按迭代组织请求的进入与退出，有助于利用计算资源；具体调度还受token预算、内存和实现约束影响。吞吐与单请求延迟之间有权衡，不能无限增加并发。图中的卡槽仅表示调度直觉，不是一请求固定占一个物理核心。',
      ], ['有长有短的请求混在一起，为什么不能只用一个平均耗时判断调度效果？', '平均数会隐藏长尾等待和不同长度请求的体验。还应观察首token时间、生成速度、延迟分位数及完成率，并保持可比较的负载。'], '连续批处理动态复用处理机会，效果要结合吞吐和等待时间共同判断。', img('vllm-basics', 'batching', '完成的任务离开后，新任务进入空位，其余请求继续；实际调度以计算与内存预算为约束。')),
    ] },
    { title: '用测量决定部署配置', lessons: [
      l('PagedAttention 关注缓存空间如何管理', [
        '资料大小不同，若每份都预留一整块很大的柜格，就容易浪费空间。小雪把资料按块安排并记录对应位置。PagedAttention借用了分页管理的思路，组织不同请求的KV缓存。',
        '逻辑上的连续序列不必占用一整段连续物理存储，块映射有助于减少不必要的预留与碎片。它不会让显存变成无限大，也不保证任何负载都有固定倍数加速。理解“更有效地管理已有内存”比背宣传数字更有用。',
      ], ['改进KV缓存分配后，是否就可以把最大上下文和并发都调到任意大？', '不可以。物理内存、计算时间和其他缓冲仍有限，配置还需依据模型、硬件和真实负载验证。减少浪费不等于取消资源上限。'], '分页式缓存管理改善空间利用，硬件容量与服务预算依旧存在。'),
      l('记录代表性负载，再决定参数', [
        '校园助手可能大多回答短问题，也可能经常阅读长资料。小雪先准备代表性的输入输出长度与并发，再记录运行环境，避免用一个特别简单的请求代表所有场景。',
        '比较配置时固定模型、精度、硬件和测试集合，记录预热条件、吞吐、首token等待、后续生成与错误情况。还要检查输出质量和权限边界。具体启动参数以当前文档为准，课程的目标是教你知道该测什么，而不是记一条过期命令。',
      ], ['要比较两个推理配置，若一个测试短输入、另一个测试长输入，结果能直接说明谁更快吗？', '不能。负载不同会影响计算与缓存需求，应在相同代表性集合和环境下比较，并说明吞吐、延迟与错误率各自的变化。'], '推理部署结论必须绑定模型、环境和负载，不能脱离条件比较一个速度数字。'),
    ] },
  ] },
]
